function run(a, b) {
    for (let i = 1; i <= a; i++) {
        for (let j = 1; j <= b; j++) {
            console.log(`${i} * ${j}=${i * j}`);
        }
    }
}

/* run(5, 9);
 */

function makePizza(size = 10) {}
